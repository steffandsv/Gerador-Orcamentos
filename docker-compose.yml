version: '3.8'

services:
  app:
    build: .
    container_name: gerador-orcamentos
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env
    networks:
      - orcamentos-network

networks:
  orcamentos-network:
    driver: bridge
